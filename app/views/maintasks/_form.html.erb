<%= form_for @maintask, :html => { :class => 'form-horizontal'} do |f| %>
  <% if @maintask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maintask.errors.count, "error") %> prohibited this maintask from being saved:</h2>

      <ul>
      <% @maintask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class = "row top20">
    <div class="form-group">
      <%= f.label 'Equipamento:', :class => 'col-sm-2 control-label' %>
      <div class="col-sm-6">
        <%= collection_select(:maintask, :equipment_id, Equipment.all, :id, :name, {:selected => @equipment.id}, {:class => 'form-control'}) %>
      </div>
    </div><!-- form-group -->
  </div><!-- row -->
  <div class = "row">
    <div class="form-group">  
      <%= f.label 'Tarefa:', :class => 'col-sm-2 control-label' %>
      <div class="col-sm-6">
        <%= f.text_area :task, :class => 'form-control' %>
      </div>
    </div><!-- form-group -->
  </div><!-- row -->
  <div class = "row">
    <div class="form-group"> 
      <%= f.label 'Peças a mudar:', :class => 'col-sm-2 control-label' %>
      <div class="col-sm-6">
        <%= f.text_area :parts, :class => 'form-control' %>
      </div> 
    </div><!-- form-group -->
  </div><!-- row -->
  <div class = "row">
    <div class="form-group"> 
      <%= f.label 'Periodicidade:', :class => 'col-sm-2 control-label' %>

      <div class="col-sm-2">
        <%= f.number_field :period, :class => 'form-control' %>
      </div>
      <div class="col-sm-2">  
        <%= f.select(:unit, options_for_select(['Horas', 'Meses', 'Dias'], @maintask.unit), {}, :class => 'form-control') %></p>
      </div>
    </div><!-- form-group -->
  </div><!-- row -->
  <div class = "row">
    <div class="form-group"> 
        <%= f.label 'Observações:', :class => 'col-sm-2 control-label' %>
        <div class="col-sm-6">
          <%= f.text_area :obs, :class => 'form-control' %>
        </div>
    </div><!-- form-group -->
  </div><!-- row -->
  <div class = "row">
    <div class="form-group">
      <div class="actions">
        <%= f.submit "Salvar", :class => 'btn btn-primary' %> 
      </div>
    </div><!-- form-group -->
  </div><!-- row -->
<% end %>
