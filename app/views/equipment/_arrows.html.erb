    <% if params[:id] %>
        <% if @equipment.previous(current_user.plant_id, @filter) %>
            <%= link_to '', @equipment.previous(current_user.plant_id, @filter), :class => 'icon-chevron-left', :title => 'Equipamento Anterior' %> |
        <% end %>
        <%= link_to '', equipment_index_path, :class => 'icon-chevron-up', :title => 'Voltar' %>
        <% if @equipment.next(current_user.plant_id, @filter) %>
            | <%= link_to '', @equipment.next(current_user.plant_id, @filter), :class => 'icon-chevron-right', :title => 'Equipamento Seguinte' %>
        <% end %>
    <% end %>