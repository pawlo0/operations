<% if params[:id] %>
    <% if @intervention.previous %>
        <%= link_to '', equipment_intervention_path(@intervention.equipment_id, @intervention.previous), :class => 'icon-chevron-left', :title => 'Intervenção Anterior' %> |
    <% end %>
    <%= link_to '', equipment_path(@intervention.equipment_id), :class => 'icon-chevron-up', :title => 'Voltar' %>
    <% if @intervention.next %>
        | <%= link_to '', equipment_intervention_path(@intervention.equipment_id, @intervention.next), :class => 'icon-chevron-right', :title => 'Intervenção Seguinte' %>
    <% end %>
<% end %>