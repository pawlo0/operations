<h1>Editar intervenção</h1>

<%= render 'form' %>

<%= link_to 'Show', equipment_intervention_path(@intervention.equipment_id, @intervention) %> |
<%= link_to 'Back', equipment_path(@intervention.equipment_id) %>
